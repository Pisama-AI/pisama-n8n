For the contract failure, Pisama proposes a deterministic input guard for operator review. The proposal names the failing consumer and stays under human review. Two real requests test the installed guard. Malformed input stops before the consumer. Valid input passes. Both results remain attached to the repair record. Rollback restores the workflow and preserves verification history.
